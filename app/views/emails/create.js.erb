document.getElementById("objects-container").insertAdjacentHTML("beforeend", "<%= j render 'email_object', email: @email %>");
$("#email-object-<%=@email.id%>").click(function(){
  $.ajax({
    url: "<%=email_path(@email.id)%>",
    type:"GET",
    dataType: "script",
    success: function(result) {
      console.log(result)
      eval(result)
    },
    error: function(error) {
      console.log(`Error ${error}`)
    }
  })
})

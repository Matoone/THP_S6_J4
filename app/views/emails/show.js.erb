<%# if(!bodiesContainer) {
  const bodiesContainer = document.getElementById("bodies-container")
} %>

document.getElementById("bodies-container").innerHTML = ""


document.getElementById("bodies-container").insertAdjacentHTML("beforeend", "<%= j render 'email_body', email: @email %>")
document.getElementById("email-object-<%=@email.id%>").classList.remove("font-weight-bold")

